<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.huorong.dao.ArticleDao">
    <!--传入单个参数 String name #{name}-->
    <select id="checkCookieExist" resultMap="java.lang.Integer" parameterType="java.lang.String">
        SELECT count(*)
        FROM user_record
        WHERE cookie = #{cookie}
    </select>

    <select id="selectArticleList" resultMap="com.huorong.domain.Article" parameterType="java.lang.String">
        SELECT
                ua.id 'id',
                u.name 'author',
                ua.create_time 'time',
                ua.article_title 'title',
                ua.article_body 'body'
        FROM user u, user_article ua
        WHERE u.id = ua.user_id
              AND u.id = #{id}
    </select>


</mapper>